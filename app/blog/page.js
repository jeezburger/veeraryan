'use client';

import BlogBrowser from '@/components/BlogBrowser';

export default function BlogPage() {
    return (
        <div style={{ width: '100vw', minHeight: '100vh', background: '#111', color: '#fff', padding: '2rem' }}>

            <BlogBrowser />
        </div>
    );
}
